//
// DO NOT EDIT THIS FILE, IT HAS BEEN GENERATED USING AndroidAnnotations.
//


package com.example.netutilssamples.itemviews;

import android.app.Activity;
import android.content.Context;
import android.util.AttributeSet;
import android.view.View;
import android.view.View.OnClickListener;
import android.widget.TextView;
import com.example.netutilssamples.R.id;


/**
 * We use @SuppressWarning here because our java code
 * generator doesn't know that there is no need
 * to import OnXXXListeners from View as we already
 * are in a View.
 * 
 */
@SuppressWarnings("unused")
public final class PersonItemView_
    extends PersonItemView
{

    private Context context_;
    private boolean mAlreadyInflated_ = false;

    public PersonItemView_(Context context) {
        super(context);
        init_();
    }

    public PersonItemView_(Context context, AttributeSet attrs) {
        super(context, attrs);
        init_();
    }

    public PersonItemView_(Context context, AttributeSet attrs, int defStyle) {
        super(context, attrs, defStyle);
        init_();
    }

    private void init_() {
        context_ = getContext();
        if (context_ instanceof Activity) {
            Activity activity = ((Activity) context_);
        }
    }

    private void afterSetContentView_() {
        count = ((TextView) findViewById(id.count));
        {
            View view = findViewById(id.plus);
            if (view!= null) {
                view.setOnClickListener(new OnClickListener() {


                    @Override
                    public void onClick(View view) {
                        PersonItemView_.this.plus();
                    }

                }
                );
            }
        }
        {
            View view = findViewById(id.minus);
            if (view!= null) {
                view.setOnClickListener(new OnClickListener() {


                    @Override
                    public void onClick(View view) {
                        PersonItemView_.this.minus();
                    }

                }
                );
            }
        }
    }

    /**
     * The mAlreadyInflated_ hack is needed because of an Android bug
     * which leads to infinite calls of onFinishInflate()
     * when inflating a layout with a parent and using
     * the <merge /> tag.
     * 
     */
    @Override
    public void onFinishInflate() {
        if (!mAlreadyInflated_) {
            mAlreadyInflated_ = true;
            afterSetContentView_();
        }
        super.onFinishInflate();
    }

    public static PersonItemView build(Context context) {
        PersonItemView_ instance = new PersonItemView_(context);
        instance.onFinishInflate();
        return instance;
    }

    public static PersonItemView build(Context context, AttributeSet attrs) {
        PersonItemView_ instance = new PersonItemView_(context, attrs);
        instance.onFinishInflate();
        return instance;
    }

    public static PersonItemView build(Context context, AttributeSet attrs, int defStyle) {
        PersonItemView_ instance = new PersonItemView_(context, attrs, defStyle);
        instance.onFinishInflate();
        return instance;
    }

}
